<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="AppTextStyle" parent="Widget.AppCompat.TextView">
        <item name="textAllCaps">false</item>
        <item name="android:fontFamily">@font/caviardreams</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <style name="SignTextStyle" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/textColor</item>
        <item name="textAllCaps">false</item>
        <item name="android:fontFamily">@font/caviardreamsbold</item>
    </style>

</resources>
